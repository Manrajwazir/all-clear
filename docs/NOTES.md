1. PPE detection weights sourced from VoxDroid/Construction-Site-Safety-PPE-Detection (MIT license). YOLOv8s, 200 epochs, 95% precision / 80% recall on Roboflow Construction Site Safety dataset (2,801 images, 10 classes).
2. the pre trained model's reported mAP is 84.1%. Precision of 92.7 and Recall 77.4. the dataset is publicly available on Roboflow.
3. YOLO is a object detection model, so it will detect objects in the image and return the bounding box of the object and the class of the object. so the result is not just whether the object is present or not, but also where it is in the image.
4. no doubts yet
