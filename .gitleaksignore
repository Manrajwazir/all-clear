# Findings reviewed by a human and judged NOT to be secrets.
# One line per finding: <commit>:<file>:<rule>:<line>. Fingerprint-exact, so
# a real key added anywhere else, even in the same file, still fails the scan.
#
# 2026-09-25. `check("idempotency_key stored", row1.idempotency_key === ...)`
# is a test assertion comparing two fields. The generic-api-key rule matched
# the word "key" next to a long identifier. There is no value on the line.
280ddf3eafc29e9baa27da05ea8f954662912411:dashboard/scripts/test-violations.mjs:generic-api-key:280
