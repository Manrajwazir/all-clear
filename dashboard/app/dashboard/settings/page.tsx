import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      eyebrow="Settings"
      title="Alert recipients & schedule"
      body="Multi-recipient SMS routing, working-hours config, retention policies. Phase 5+."
    />
  );
}
