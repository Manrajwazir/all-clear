import { ComingSoon } from "@/components/layout/ComingSoon";

export default function CamerasPage() {
  return (
    <ComingSoon
      eyebrow="Cameras"
      title="Camera management"
      body="Register IP cameras, set RTSP URLs, and assign them to sites. Single-camera MVP for now."
    />
  );
}
