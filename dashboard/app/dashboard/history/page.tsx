import { ComingSoon } from "@/components/layout/ComingSoon";

export default function HistoryPage() {
  return (
    <ComingSoon
      eyebrow="History"
      title="Long-range archive"
      body="Filter, search, and export resolved violations beyond the live feed window. Available in Phase 5."
    />
  );
}
